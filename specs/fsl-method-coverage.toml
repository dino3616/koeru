# 収録方式ごとの書き出し可否。
#
# 方式の上下関係は宣言しない。 エイリアス被覆から導出する（TR-PKG-22 / TR-PKG-23）。
# 以前は有向グラフとして宣言していたが、同じ命題が被覆判定と2箇所にできていた。
# 書き出しの関門そのものは fsl-packaging-export.toml が持つ。

[requirements]
file = "requirements/method-coverage.fsl"
depth = 12
