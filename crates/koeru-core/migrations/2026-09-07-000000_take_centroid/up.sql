-- 声の色のもとになるスペクトル重心（DEC-PLT-025）。
--
-- take_analysis に足す。 F0 と振幅は既にここにあり、色を作るのに要る3つ
-- （音高帯・共鳴の重心・発声の長さ）のうち重心だけが欠けていた。
-- 別表にすると、色を作るたびに2つの表を突き合わせることになる。
--
-- NULL を許す。 この列より前に録ったテイクは値を持たない。
-- 0 で埋めると「重心が 0 Hz」と区別できず、色が黙って寄る。
ALTER TABLE take_analysis ADD COLUMN centroid_hz REAL;
