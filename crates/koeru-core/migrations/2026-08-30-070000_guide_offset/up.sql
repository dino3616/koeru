-- ガイド再生と録音の時刻対応（TR-REC-26）。
--
-- 「ガイドのフレーズ開始が、録音の何サンプル目に相当するか」。
-- 負にもなる（ガイドがテイクの先頭より前に始まっていた場合）。
--
-- 参考値としてのみ用いる。 音素境界の切り出しやトリミングの根拠にしない。
-- 入力デバイスと出力デバイスが別なら、クロックドリフトが生じる。
-- テイクをまたいだ累積補正は行わない。
--
-- NULL は「そのテイクの間、ガイドが鳴っていなかった」。単独音では常にこれ。
ALTER TABLE take_metrics ADD COLUMN guide_offset_frames INTEGER;
