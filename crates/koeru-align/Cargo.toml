[package]
name = "koeru-align"
version = "0.0.0"
publish = false
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
description = "KOERU の自動原音設定。MFA の音響モデルを Kaldi 経由で叩き、oto の5値を導く"

[build-dependencies]
cc.workspace = true

[dev-dependencies]
koeru-audio = { path = "../koeru-audio" }

[lints]
workspace = true

[dependencies]
koeru-core = { path = "../koeru-core" }
koeru-failure = { path = "../koeru-failure" }
koeru-formats = { path = "../koeru-formats" }
koeru-model = { path = "../koeru-model" }
# 同梱モデルの `meta.json` から版を読む（TR-ALN-29 の指紋）。
serde_json.workspace = true
sha2.workspace = true
thiserror.workspace = true
toml_edit.workspace = true
tracing.workspace = true
