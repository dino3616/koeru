//! KOERU が読み書きする外部形式（`DEC-PLT-034`）。
//!
//! 構文と符号化だけを置く。 `oto.ini` の行、周波数表（`.frq`）のバイト列、
//! CP932 / UTF-8 の読み書き。 どれを採用するか、台帳のどれと対応するか、確信度を
//! どう扱うかは持たない。
//!
//! 入出力を持たない。 受け取るのはバイト列と文字列で、ファイルを読み書きするのは
//! 呼び出し側。 依存とソースの純度は `tests/purity.rs` が見る。
//!
//! 移行中。 `koeru_core::text` / `koeru_core::frq` / `koeru_align::ini` が同じものを
//! 再輸出しているので、既存の経路はそのまま通る。

pub mod frq;
pub mod oto_ini;
pub mod text;
