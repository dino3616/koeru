schema = 'question'
id = 'Q-PLT-002'
title = '予算表に存在しない工程が7つある'
status = 'open'
owner = 'dino3616'
why_it_matters = '''
新設が必要な項目。**いずれも現在どの領域の予算表にも存在しない。**
以下の工程は、どの領域の予算表にも行が無い。行が無いものは詰められない。

- プロセス全体 1.5GB の内訳表（実行モード別）（由来: Q-PLT-001）
- スペクトログラムキャッシュのディスク使用量（由来: Q-EDT-001）
- 全件再推定後のフレーズキャッシュ再構築（由来: 上記）
- 出力デバイス open ＋ 初回バッファ投入（由来: E2E の押下後経路）
- 録音直後のオフライン前処理（由来: DEC-REC-003）
- 下位方式書き出し時の5値再導出（由来: align の新設要件）
- 全件再推定時の master WAV 読み直し帯域（由来: 上記）
'''
how_to_close = '各行を担当する領域を決め、目標値を置いてから実測に差し替える。'
blocks_profiles = [
  'PROFILE-M1',
]
affects_budgets = ['BUDGET-MEMORY-001', 'BUDGET-LATENCY-001']
