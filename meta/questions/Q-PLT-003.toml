schema = 'question'
id = 'Q-PLT-003'
title = 'アクセシビリティを既定要件と決めたのに、要件が1件も無い'
status = 'open'
owner = 'dino3616'
why_it_matters = '''
スクリーンリーダー対応、キーボードのみでの操作完結、コントラスト。
**既定で満たす要件と決めたのに、7領域のどこにも要件が無い。**
ショートカット中心の設計はキーボード操作と親和的なので、要件化のコストは低い。
'''
how_to_close = 'Tauri の WebView が OS のアクセシビリティ API へ何を渡すかを確かめ、TR-PLT 領域へ要件として追加する。'
blocks_profiles = [
  'PROFILE-M1',
]
affects_requirements = [
  'TR-PLT-24',
]

# 移行前の原文。言い換えで意味が動いていないかを突き合わせるために残す。
source_text = '| アクセシビリティ | スクリーンリーダー対応、キーボードのみでの操作完結、コントラスト。**既定要件と決めたのに要件が無い。** ショートカット中心の設計はキーボード操作と親和的なので、要件化のコストは低い |'
