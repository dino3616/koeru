id = 'Q-ALL-002'
title = '確定した方針に、要件側の書き換えが追いついていない箇所がある'
status = 'open'
owner = 'dino3616'
why_it_matters = '''
方針が後から決まったため、要件が古い前提のまま残っている。**方針が正で、要件を直す。**

- **書き出すファイル名は ASCII 固定**（読み込みは日本語も対応）: TR-RCL-08 と TR-RCL-18 が日本語ファイル名を前提にしている。ASCII 固定は TR-PKG-11 / 14 / 39 / 45 と TR-EDT-42 を解く代わりにこの2件を壊すので、RCL 側を書き換える
- **合成は WORLD ベース、F0 推定のみ SwiftF0**: SYN 領域の要件が Harvest 前提で書かれている箇所がある
- **`.frq` は録音時に作る**: PKG 側に書き出し時生成を前提とした記述が残っていないか確認する
'''
how_to_close = '該当要件の statement を書き換える。登録簿に移したので、ID から直接引ける。'
blocks_profiles = []
affects_requirements = [
  'TR-RCL-08',
  'TR-RCL-18',
  'TR-PKG-11',
  'TR-PKG-14',
  'TR-PKG-39',
  'TR-PKG-45',
  'TR-EDT-42',
]

# 移行前の原文。言い換えで意味が動いていないかを突き合わせるために残す。
source_text = '''
確定方針が後から決まったため、要件側の書き換えが済んでいないもの。**方針が正で、要件を直す。**
| **書き出すファイル名は ASCII 固定**（読み込みは日本語も対応） | TR-RCL-08 と TR-RCL-18 が日本語ファイル名を前提にしている。ASCII 固定は TR-PKG-11 / 14 / 39 / 45 と TR-EDT-42 を解く代わりにこの2件を壊すので、RCL 側を書き換える |
| **合成は WORLD ベース、F0 推定のみ SwiftF0** | SYN 領域の要件が Harvest 前提で書かれている箇所がある |
| **`.frq` は録音時に作る** | PKG 側に書き出し時生成を前提とした記述が残っていないか確認する |
'''
