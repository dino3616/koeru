id = 'Q-ALL-001'
title = '「3時間の多音階連続音」の規模が領域ごとに3つの異なる数値で想定されている'
status = 'open'
owner = 'dino3616'
why_it_matters = '''
同じプロジェクトを指していない。**予算の突き合わせが成立しない。**
プロセス全体のメモリ予算も、この規模が確定しないと積み直せない。
'''
how_to_close = '代表プロジェクトの規模（項目数・総音声長・総容量）を1つ決め、全領域の予算をそこから導く。'
blocks_profiles = []
affects_requirements = [
  'TR-RCL-09',
]
affects_questions = [
  'Q-PLT-001',
]
affects_budgets = [
  'BUDGET-MEMORY-001',
  'BUDGET-LATENCY-001',
]

# 移行前の原文。言い換えで意味が動いていないかを突き合わせるために残す。
source_text = '''
**261件のうち、領域を跨ぐ `depends_on` を宣言しているものは0件である。** その結果、同じアルゴリズムが2つの領域で別々に定義され、内容が食い違っている。
| 「3時間の多音階連続音」の規模 | **領域ごとに3つの異なる数値で想定されており、同じプロジェクトを指していない。** 予算の突き合わせが成立しない | TR-RCL-09、PLT / EDT / PKG / REC の各予算 |
**[Risk]** 依存が宣言されていないため、片方の要件を直したときにもう片方が壊れることを検出できない。**要件 ID 間の依存関係を明示することが、他のどの修正よりも先に来る。**
'''

# 移行前に、この論点が置かれていた節の導入文。
section_lead = '**いずれも実装着手前に解消しないと、2つの領域が別々のものを作る。**'
