schema = 'evidence'
id = "EVID-LIC-001"
title = "同梱候補のライセンス確認"
kind = "license_audit"
source = "deny.toml と cargo deny check、および各上流リポジトリの原文"
provenance = "CI が依存全体を機械判定する。許可リストに無いライセンスは「不明」として失敗する"
confidence = "Fact"
supports_decisions = ["DEC-SYN-001", "DEC-ALN-001", "DEC-ALN-004", "DEC-ALN-008", "DEC-ALN-012"]
open_items = [
  "Microsoft Speech Language Translation Japanese のライセンス原文（MFA 日本語音響モデルの学習コーパスの1つ。→ EVID-ALN-001）",
  "**LaboroTV Non-commercial の条項原文。学習量の 91.7% を占めるので、いちばん重い**（→ DEC-ALN-012）",
]
note = "学習済みモデルは、モデル側の表示ライセンスが学習コーパスの条件を上書きできるとは限らない。"

# 監査を通した結果。
audited = [
  '**MFA 日本語音響モデル v3.0.0: 判断で通した**（DEC-ALN-008）。**モデル自身の表示は CC BY 4.0** で mfa-models のモデルカードに明記されている。**[Risk] 学習コーパス5つのうち GlobalPhone Japanese v3.1 は ELRA の有償ライセンス（研究用と商用で料金が分かれ、再配布不可）、TEDxJP-10K v1.1 は TED / TEDx 由来で CC BY-NC-ND（非商用かつ改変不可）。** Common Voice ja v12.0 は CC0、JVS は比較的軽い。Microsoft Speech Language Translation Japanese は未確認。**学習コーパスの条件が派生した重みへ及ぶかは法的に定まっていない。配布者の表示を額面どおり受け取る判断であって、確実性の主張ではない。** CC BY 4.0 は帰属表示が義務なので、アプリ内のライセンス表記に MFA と5つのコーパスを載せる。→ EVID-ALN-001',
  '**Julius segmentation-kit: 判断で通した**（DEC-ALN-004）。`julius-speech/segmentation-kit` の License.md は MIT で、音響モデル（`hmmdefs_monof_mix16_gid.binhmm` 464KB / `hmmdefs_ptm_gid.binhmm` 2.9MB / `logicalTri` 239KB）はそのリポジトリの中にある。**[Risk] ただし License.md は対象を "the Software" と定義するだけで `.binhmm` を明示的に列挙しておらず、SwiftF0 の `MANIFEST.in` のような「著者が重みを意図して同梱している」痕跡が無い。学習コーパスの出所も未確認。** **リポジトリに同梱されている事実を受け取る判断であって、確実性の主張ではない。** 上流への問い合わせは継続し、否定されたら snfa へ戻す',
  '**SwiftF0: 判断で通した**（DEC-SYN-004）。**重みは MIT で明示されている** — `lars76/swift-f0` の LICENSE に例外規定が無く、`model.onnx` がリポジトリ内にあり `MANIFEST.in` が明示的に同梱している。**[Risk]** 学習コーパス5つのうち MDB-stem-synth が CC BY-NC 4.0（元の MedleyDB は CC BY-NC-SA 3.0）、MIR-1K はライセンス非公開。NSynth（CC BY 4.0）と PTDB-TUG（ODbL）は問題ない。**学習コーパスの条件が派生した重みへ及ぶかは法的に定まっていない。著者の表示を額面どおり受け取る判断であって、確実性の主張ではない**',
]

# 技術的な比較の余地があると誤解しないための記録。
ruled_out_by_license = [
  '文字コード判定（uchardet 系）: MPL-1.1 が TR-PLT-22 の台帳ルールと字面で衝突する。**自前実装で落とせる。** UTAU 音源は CP932 と UTF-8 の2択なので汎用判定器は不要',
  'LGPL 部品（alsa-lib、libsoxr）: 直接ダウンロード配布なら LGPL-2.1 の差し替え要件は満たせる。ただし libsoxr は libsamplerate（BSD-2-Clause）で置換でき、alsa-lib は Linux バックエンドが `libasound` を実行時 dlopen するため配布物に入れる必要がない（DEC-REC-001 で各 OS の API を直接叩くことにしたので、miniaudio は使わない）。**[Risk]** AppImage 作成時に依存収集ツールが `libasound.so.2` を自動で取り込む経路があるので、除外リストに入れ、生成後に `ldd` と同梱ファイル一覧を CI で検査する',
  'ライセンス不明の部品（moresampler / setParam / resampler.exe）: **「調査中」ではなく「全権利留保」として扱う。** 逆コンパイル禁止の線を明示する',
]
