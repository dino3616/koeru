schema = 'decision'
id = 'DEC-ALL-005'
constraint_label = '既存ツールの参照方法'
constraint = '既存ツールからは機能的事実だけを参照し、ソースを読まず取り込まない'
title = 'コピーレフトの既存ツールは、公開された機能的事実からのみ互換基準を定める'
status = 'accepted'
owner = 'dino3616'
options = [
  '機能的事実のみ参照する',
  'ソースを読んで互換実装する',
  '参照しない',
]
selected = '機能的事実のみ参照する'
rationale = '''
setParam、snack、KiraOtoToolbox はいずれも GPL。**互換性のために振る舞いを揃えたいが、
ソースを読んで書くと派生物になる疑いが残る。**

参照してよいのは、UI 文字列、公開されているキーバインド一覧、機能リスト、
入出力ファイル形式といった公開文書から読み取れる事実だけ。 実装は独立に行う。

台帳の status を「不適」にしているのは、**部品として取り込まないという意味であって、
互換基準の参照までを禁じるものではない。** 参照の範囲は各部品の note が持つ。

moresampler も同じ扱いだが、ライセンス自体が不明なため DEC-ALL-002 側で外れる。
逆コンパイルと、出力の系統的観察によるアルゴリズム再現は行わない。
'''
review_triggers = [
  '上流が許容的なライセンスへ変更したとき',
  '互換基準が公開文書だけでは定まらないと分かったとき',
]
affects_components = [
  'CMP-022',
  'CMP-023',
  'CMP-024',
]
