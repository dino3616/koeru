id = 'DEC-PLT-008'
title = '録音品質のリアルタイム判定・プロトタイプ検証フェーズ・UI の多言語対応をスコープ外にする'
constraint_label = 'スコープ外'
constraint = '録音品質のリアルタイム判定。プロトタイプ検証フェーズ。UI の多言語対応'
status = 'accepted'
owner = 'dino3616'
options = [
  '3つともスコープ外',
  'リアルタイム判定を入れる',
  '多言語対応を入れる',
]
selected = '3つともスコープ外'
rationale = '''
**録音品質のリアルタイム判定**は、評価・警告・スコアを出すことが「まず歌える」を妨げるため取らない。
計測は行うが判定しない。**プロトタイプ検証フェーズ**は置かず、最初から本番として作る。
**UI の多言語対応**は初期スコープ外。UTAU 文化圏は日本語中心である一方、
OpenUtau / vLabeler / RecStar はいずれも多言語対応している点は認識している。
'''
review_triggers = [
  '壊れた成果物が完成に到達する経路が、書き出し前の関門だけでは塞げないと判明したとき',
  '日本語圏以外からの要望が無視できない規模になったとき',
]
affects_requirements = [
  'TR-REC-16',
]
